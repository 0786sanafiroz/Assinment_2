//print size of int,float,char,double.
#include<stdio.h>
int main()
{
 printf("the size of int in bytes is %d\n",sizeof(int));
 printf("the size of float in bytes is %d\n", sizeof(float));
 printf("the size of char in bytes is %d\n",sizeof(char));
 printf("the size of double in bytes %d\n",sizeof(double));
 return 0;
 }
